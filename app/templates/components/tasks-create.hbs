<div class=current-user-background>
  <div class="users-current-nav">
    <h1 class="current-user-heading">
      MAMMOTH<span> task</span>
    </h1>

    <h5 class="greeting">Hello {{model.name}}</h5>

    <div class="pure-menu pure-menu-horizontal">
      <ul class="pure-menu-list">
        <li class="pure-menu-item pure-menu-selected"><a href="#" class="pure-menu-link">Manage</a></li>
        <li class="pure-menu-item pure-menu-selected"><a href="#" class="pure-menu-link">How to Use</a></li>
        <li class="pure-menu-item pure-menu-selected"><a href="#" class="pure-menu-link">About</a></li>
        <li class="pure-menu-item pure-menu-has-children pure-menu-allow-hover">
          <a href="#" id="menuLink1" class="pure-menu-link">Contact</a>
          <ul class="pure-menu-children">
            <li class="pure-menu-item"><a href="#" class="pure-menu-link">Email</a></li>
            <li class="pure-menu-item"><a href="#" class="pure-menu-link">Twitter</a></li>
            <li class="pure-menu-item"><a href="#" class="pure-menu-link">Github</a></li>
          </ul>
        </li>
      </ul>
    </div>

    <div class="log-out-header">
      {{#if session.isAuthenticated}}
        <button class="pure-button log-out" name="button" {{action 'invalidateSession'}}>Log Out</button>
      {{/if}}
    </div>
  </div>
</div>

<div class="task-create-container">
  <div class="task-create">
    <p class="task-instruction">
      Create a large Project with a Due date. <br>
      Add and remove subtask to that Project...<br>
      MAMMOTH will break down the Project into small managable chunks.
    </p>
    <form class="pure-form pure-form-stacked task-form" {{action "createTask" "addEvent" on="submit"}}>
      <p class="task-instruction">
        Create a large Project with a Due date. <br>
        Add and remove subtask to that Project...<br>
        MAMMOTH will break down the Project into small managable chunks.
      </p>
      <fieldset>

        <legend>Project</legend>
        {{input type='text' placeholder='Project Name' value=model.taskName}}
        {{!-- {{input placeholder="Due date" type="date" value=model.dueDate }} --}}
        {{date-picker dateValue=model.dueDate}}

      </fieldset>
      <button class="pure-button button-main save-project" type="submit" name="button">Save Project</button>

    </form>
  </div>

  {{full-calendar events=events}}

</div>

{{yield}}
